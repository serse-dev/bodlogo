"""
Физик/Математик Бодлого Үүсгэгч - Gemini AI

Энэхүү програмыг Google Gemini AI ашиглан 
физик, математикийн бодлого үүсгэхэд зориулав.
"""

import os
import streamlit as st
import google.generativeai as genai
from typing import Generator

# ------------------------------
# Хичээл ба дэд сэдвүүдийн тодорхойлолт
# ------------------------------
PHYSICS_SUBTOPICS = {
    "Механик": ["Кинематик", "Динамик", "Статик", "Гравитаци", "Хөдөлгөөний хадгалалтын хууль", "Хүчний момент"],
    "Термодинамик": ["Хийн хууль", "Дулаан дамжуулалт", "Дулааны машин", "Энтропи", "Термодинамикийн хууль"],
    "Цахилгаан ба Соронз": ["Цахилгаан орон", "Цахилгаан гүйдэл", "Соронзон орон", "Цахилгаан соронзон индукц", "RC ба RL хэлхээ"],
    "Долгион ба Оптик": ["Долгионы шинж чанар", "Гэрлийн огилт", "Гэрлийн интерференц", "Гэрлийн туялзуур", "Хазайлт"],
    "Орчин үеийн физик": ["Квант механик", "Харьцангуй онол", "Атомын физик", "Цөмийн физик", "Элементар бөөмс"]
}

MATH_SUBTOPICS = {
    "Алгебр": ["Тэгшитгэл бодох", "Тэнцэтгэл биш", "Олон гишүүнт", "Комплекс тоо", "Матриц, тодорхойлогч"],
    "Геометр": ["Гурвалжин", "Дөрвөлжин", "Тойрог", "Геометр трансформац", "Стереометр"],
    "Тригонометр": ["Тригонометр функц", "Тригонометр тэгшитгэл", "Тригонометр тэнцэтгэл биш", "Инверс тригонометр функц"],
    "Математик анализ": ["Уламжлал", "Интеграл", "Дифференциал тэгшитгэл", "Функцийн судалгаа", "Ряд"],
    "Магадлал ба Статистик": ["Комбинаторик", "Магадлалын онол", "Санамсаргүй хэмжигдэхүүн", "Статистик дүн анализ", "Регрессийн анализ"]
}

# ------------------------------
# Streamlit тохиргоо
# ------------------------------
st.set_page_config(
    page_title="Физик/Математик Бодлого Үүсгэгч",
    page_icon="🧮",
    layout="centered",
    initial_sidebar_state="expanded"
)

# ------------------------------
# Хэрэглэгчийн интерфейс
# ------------------------------
st.title("🧮 Физик/Математик Бодлого Үүсгэгч")
st.caption("Google Gemini AI ашиглан сонгосон сэдвээр 10 шинэ бодлого үүсгэнэ")

# Түр зуур API түлхүүргүй үед загвар харуулах
DEMO_MODE = False

# ------------------------------
# Хажуу самбар - Тохиргоо
# ------------------------------
with st.sidebar:
    st.header("⚙️ Тохиргоо")
    
    # API түлхүүр оруулах
    api_key = st.text_input(
        "Google API Key",
        type="password",
        help="Google AI Studio-аас авах API түлхүүрээ оруулна уу",
        value=st.session_state.get("api_key", "")
    )
    
    if api_key:
        st.session_state["api_key"] = api_key
        os.environ["GOOGLE_API_KEY"] = api_key
        DEMO_MODE = False
    else:
        st.warning("API түлхүүр оруулаагүй байна. Демо горимд ажиллаж байна.")
        DEMO_MODE = True
    
    # Хичээл сонгох
    subject = st.selectbox(
        "Хичээл",
        ["Физик", "Математик"],
        index=0
    )
    
    # Гол сэдэв сонгох
    if subject == "Физик":
        main_topic = st.selectbox(
            "Гол сэдэв",
            list(PHYSICS_SUBTOPICS.keys()),
            index=0
        )
        subtopic = st.selectbox(
            "Дэд сэдэв",
            PHYSICS_SUBTOPICS[main_topic],
            index=0
        )
    else:
        main_topic = st.selectbox(
            "Гол сэдэв",
            list(MATH_SUBTOPICS.keys()),
            index=0
        )
        subtopic = st.selectbox(
            "Дэд сэдэв",
            MATH_SUBTOPICS[main_topic],
            index=0
        )
    
    # Үүсгэх тоо
    problem_count = st.slider("Үүсгэх бодлогын тоо", 1, 20, 10)
    
    # Шийд оруулах эсэх
    include_solutions = st.checkbox("Шийд оруулах", value=True)
    
    # Температур
    temperature = st.slider(
        "Санаачилга (temperature)", 
        0.0, 1.0, 0.7, 0.1,
        help="Өндөр байх тусам илүү бүтээлч боловч баталгаажсан байдал буурна"
    )
    
    st.markdown("---")
    st.subheader("🔄 Жишээ бодлого")
    
    # Жишээ бодлогууд
    if subject == "Физик":
        if main_topic == "Механик":
            if subtopic == "Кинематик":
                example_problem = "Машин 72 км/ц хурдтай явж байгаад 4 секундын дотор зогссон. Машины хурдатгал болон зогсох зам нь хэд вэ?"
            elif subtopic == "Динамик":
                example_problem = "15° налуу хавтгай дээр 2 кг масстай биетийг үрэлтгүй орчинд чирэхэд биед үйлчлэх татах хүч болон налуугийн дагуух хурдатгалыг ол. g=9.8 м/с²."
            else:
                example_problem = "5 кг масстай бие 10 м/с хурдтайгаар хөдөлж байгаад 2 кг масстай тайван байгаа биетэй мөргөлдөв. Мөргөлдөөн уян хатан бол угсарсан биеийн хурдыг ол."
        elif main_topic == "Цахилгаан ба Соронз":
            example_problem = "10 Ω эсэргүүцэлтэй дамжуулагчид 12 В хүчдэл залгахад гүйдэл хэд вэ? Дамжуулагчаар 5 минут явахад ялгарах дулааны хэмжээг ол."
        else:
            example_problem = "100 г масстай биеийн температур 20°C-аас 80°C хүртэл халаахад шаардагдах дулааны хэмжээг ол. Биеийн хувийн дулаан багтаамж 0.5 J/g°C байна."
    else:
        if main_topic == "Алгебр":
            example_problem = "x² - 5x + 6 = 0 тэгшитгэлийн бодит шийдийг ол. Мөн язгууруудын нийлбэр ба үржвэрийг ол."
        elif main_topic == "Геометр":
            example_problem = "Гурвалжны талууд нь 6 см, 8 см, 10 см байна. Энэ гурвалжин тэгш өнцөгт эсэхийг шалгаад, талбайг нь ол."
        elif main_topic == "Математик анализ":
            example_problem = "y = 2x² - 4x + 1 функцийн уламжлалыг олж, экстремум цэгүүдийг тодорхойл."
        else:
            example_problem = "Нэг шоо шидэхэд: a) 4-өөс их тоо буух магадлал, b) тэгш тоо буух магадлалыг ол."
    
    if st.button("Жишээгээр дүүргэх"):
        st.session_state["problem_text"] = example_problem
        st.rerun()

# ------------------------------
# Гол контент хэсэг
# ------------------------------
st.subheader(f"{subject} - {main_topic}: {subtopic}")

# Бодлого оруулах талбар
problem_text = st.text_area(
    "Бодлогын жишээ оруулах",
    height=150,
    key="problem_text",
    placeholder=example_problem,
    help="Өөрийн бодлогыг оруулах эсвэл дээрх 'Жишээгээр дүүргэх' товчийг дарна уу"
)

# Товчлуурууд
col1, col2, col3 = st.columns([1, 1, 2])
with col1:
    generate_btn = st.button("🚀 Бодлого үүсгэх", type="primary", use_container_width=True)
with col2:
    clear_btn = st.button("🧹 Цэвэрлэх", use_container_width=True)
with col3:
    st.info(f"{problem_count} бодлого үүсгэнэ")

if clear_btn:
    st.session_state["problem_text"] = ""
    st.rerun()

# ------------------------------
# Функцууд
# ------------------------------
def setup_gemini():
    """Gemini AI тохируулах"""
    try:
        if "api_key" in st.session_state and st.session_state.api_key:
            genai.configure(api_key=st.session_state.api_key)
            return genai.GenerativeModel('gemini-pro')
        else:
            st.error("API түлхүүр оруулаагүй байна. Тохиргоо хэсгээс API түлхүүрээ оруулна уу.")
            return None
    except Exception as e:
        st.error(f"Gemini тохируулах үед алдаа гарлаа: {e}")
        return None

def generate_problems_prompt(user_problem: str, count: int = 10, with_solutions: bool = True) -> str:
    """Бодлого үүсгэх prompt бэлтгэх"""
    return f"""
Та бол {subject} хичээлийн {main_topic} сэдвийн {subtopic} дэд сэдэвт мэргэшсэн багш.

ДААЛГАВАР:
- Дараах жишээ бодлоготой ижил түвшин, ижил төрлийн {count} шинэ бодлого үүсгэ.
- Бодлого бүр өөр өөр нөхцөл, өгөгдөлтэй байх.
- Бодлого нь ойлгомжтой, нэг утгатай, шийдвэрлэх боломжтой байх.
- Хэрэглэгдэх нэгж, томьёо зөв байх.

ЖИШЭЭ БОДЛОГО:
{user_problem}

ҮҮСГЭХ БОДЛОГОНУУД:
{count} шинэ бодлого үүсгэ. Бодлого бүрийг дараах форматтайгаар гарга:

1. **Бодлого:** [бодлогын текст]
   **Сэдэв:** {main_topic} - {subtopic}
   {"**Шийд:** [бодлогын шийдэл]" if with_solutions else ""}

Бүх бодлогуудыг Монгол хэлээр үүсгэ.
"""

def stream_gemini_response(prompt: str) -> Generator[str, None, None]:
    """Gemini-ийн хариуг stream хэлбэрээр авах"""
    try:
        model = setup_gemini()
        if not model:
            yield "API түлхүүр буруу эсвэл оруулаагүй байна."
            return
            
        response = model.generate_content(
            prompt,
            stream=True,
            generation_config=genai.types.GenerationConfig(
                temperature=temperature,
                max_output_tokens=2048,
            )
        )
        
        for chunk in response:
            yield chunk.text
                
    except Exception as e:
        yield f"Алдаа гарлаа: {str(e)}"

def show_demo_problems():
    """Демо горимд жишээ бодлого харуулах"""
    st.info("Демо горимд ажиллаж байна. Бодит бодлого үүсгэхийн тулд API түлхүүрээ оруулна уу.")
    
    st.subheader("Үүсгэсэн бодлогууд (жишээ):")
    
    if subject == "Физик":
        if main_topic == "Механик" and subtopic == "Динамик":
            demo_content = """
1. **Бодлого:** 30° налуу хавтгай дээр 3 кг масстай биетийг дээш чирж байна. Үрэлтийн коэффициент 0.2 бол биетийн хурдатгалыг ол. g=9.8 м/с².
   **Сэдэв:** Механик - Динамик
   **Шийд:** Хүчдийн диаграмм зурж, Ньютоны хуулийг хэрэглэн бодно. Хариу: 3.2 м/с²

2. **Бодлого:** 5 кг масстай бие 20 Н хүчний нөлөөгөөр хөдөлж байна. 10 секундын дараа биеийн хурд хэд болох вэ? Анхны хурд 0 байна.
   **Сэдэв:** Механик - Динамик
   **Шийд:** a = F/m = 4 м/с², v = at = 40 м/с
"""
        else:
            demo_content = """
1. **Бодлого:** 200 Ом эсэргүүцэлтэй резистор дээр 24 В хүчдэл өгөгдсөн. Резистороор гүйх гүйдлийн хүчийг ол.
   **Сэдэв:** Цахилгаан ба Соронз - Цахилгаан гүйдэл
   **Шийд:** Омын хуулиар I = V/R = 24/200 = 0.12 А

2. **Бодлого:** 0.5 мТл индукцтэй соронзон орон дотор 30 см урттай дамжуулагч 10 м/с хурдтайгаар хөдөлж байна. Дамжуулагчийн үүсгэх хүчдэлийг ол.
   **Сэдэв:** Цахилгаан ба Соронз - Цахилгаан соронзон индукц
   **Шийд:** ε = B*l*v = 0.0005 * 0.3 * 10 = 0.0015 В
"""
    else:
        if main_topic == "Алгебр":
            demo_content = """
1. **Бодлого:** 2x² - 8x + 6 = 0 тэгшитгэлийн шийдийг ол.
   **Сэдэв:** Алгебр - Тэгшитгэл бодох
   **Шийд:** x = 1, x = 3

2. **Бодлого:** (x-3)(x+2) = 10 тэгшитгэлийг бод.
   **Сэдэв:** Алгебр - Тэгшитгэл бодох
   **Шийд:** x² - x - 6 = 10 → x² - x - 16 = 0 → x = (1 ± √65)/2
"""
        else:
            demo_content = """
1. **Бодлого:** y = x³ - 3x² + 2 функцийн уламжлалыг олж, экстремум цэгүүдийг тодорхойл.
   **Сэдэв:** Математик анализ - Уламжлал
   **Шийд:** y' = 3x² - 6x = 3x(x-2), x=0 ба x=2 цэгүүдэд экстремумтай

2. **Бодлого:** ∫(2x + 3) dx интегралыг бод.
   **Сэдэв:** Математик анализ - Интеграл
   **Шийд:** x² + 3x + C
"""
    
    st.markdown(demo_content)
    st.warning("Энэ бол зөвхөн демо жишээ юм. Бодит бодлого үүсгэхийн тулд API түлхүүрээ оруулна уу.")

# ------------------------------
# Үндсэн логик
# ------------------------------
if generate_btn:
    if not problem_text.strip():
        st.warning("Бодлогын жишээ оруулна уу")
    else:
        if DEMO_MODE:
            show_demo_problems()
        else:
            with st.spinner("Бодлогуудыг үүсгэж байна..."):
                prompt = generate_problems_prompt(problem_text, problem_count, include_solutions)
                
                st.subheader("Үүсгэсэн бодлогууд:")
                response_area = st.empty()
                
                full_response = ""
                for chunk in stream_gemini_response(prompt):
                    full_response += chunk
                    response_area.markdown(full_response)
                
                # Үр дүнг хадгалах
                st.session_state["last_response"] = full_response
                
                # Татаж авах боломж
                st.download_button(
                    label="📥 Үр дүнг татаж авах",
                    data=full_response,
                    file_name=f"{subject}_{main_topic}_{subtopic}_бодлогууд.md",
                    mime="text/markdown"
                )

# ------------------------------
# Мэдээлэл хэсэг
# ------------------------------
st.markdown("---")
expander = st.expander("ℹ️ Заавар")
expander.markdown("""
1. **API түлхүүр оруулах**: Google AI Studio-аас API түлхүүрээ аваад тохиргооны хэсэгт оруулна
2. **Хичээл сонгох**: Физик эсвэл Математик хичээл сонгоно
3. **Сэдэв сонгох**: Гол сэдэв болон дэд сэдвийг сонгоно
4. **Бодлого оруулах**: Өөрийн бодлогыг оруулах эсвэл жишээ бодлогыг ашиглана
5. **Үүсгэх**: "Бодлого үүсгэх" товчийг дарж үр дүнг харах

**Анхааруулга**: API түлхүүргүй үед зөвхөн демо үр дүн харуулах болно.
""")

# ------------------------------
# Хөл хэсэг
# ------------------------------
st.markdown("---")
st.caption("© 2023 Физик/Математик Бодлого Үүсгэгч - Google Gemini AI ашигласан")

# Хэрэглэгчийн тохиргоог хадгалах
if api_key:
    st.session_state["api_key"] = api_key